// Test fixture: an Android app written in the Kotlin DSL. Nothing here names the
// Android plugin; app/build.gradle.kts declares `namespace = ...`, which language
// detection has to recognize on its own.
plugins {
}
