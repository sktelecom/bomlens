# 오픈소스 고지문 빠른 시작 (라이선스 담당자용)

> **관련 문서**: [시작하기](getting-started.md) | [고지문·보안·UI](notice-security-ui-guide.md)

명령어를 한 번도 써본 적 없어도 됩니다. 이 문서는 오픈소스 라이선스 담당자가 개발팀에게
받은 소스 코드로 오픈소스 고지문을 만드는 가장 짧은 길만 담았습니다. 브라우저 화면에서
클릭만으로 끝납니다.

## 오픈소스 고지문이란

제품에 포함된 오픈소스 구성요소와 그 라이선스를 정리해 배포 시 함께 제공하는 문서입니다.
많은 오픈소스 라이선스(MIT, Apache-2.0, BSD 등)가 저작권 표시와 라이선스 전문을 제품에
동봉하도록 요구하기 때문에, 이를 모아 둔 고지문이 필요합니다.

이 도구는 소스 코드를 분석해 구성요소 목록(SBOM)을 만들고, 거기서 라이선스별로 구성요소를
묶어 고지문 두 가지를 생성합니다.

- `..._NOTICE.txt` — 배포물에 그대로 동봉하는 텍스트 형식
- `..._NOTICE.html` — 브라우저로 보기 좋은 형식

이름 앞부분(`...`)에는 입력한 프로젝트 이름과 버전이 붙습니다. 예를 들어 프로젝트가
`MyApp`, 버전이 `1.0.0`이면 `MyApp_1.0.0_NOTICE.txt`가 됩니다.

## 준비물

Docker 엔진 하나만 있으면 됩니다. Windows에서 처음 설치한다면 무료이면서 더블클릭 흐름과
잘 맞는 **Rancher Desktop**을 권장합니다. 이미 Docker를 쓰고 있다면 그대로 두고 다음으로
넘어가세요. (다른 선택지의 자세한 비교는 [시작하기](getting-started.md) 문서에 있습니다.)

## 따라 하기

### 1. Rancher Desktop 설치

[rancherdesktop.io](https://rancherdesktop.io/)에서 Windows용 설치 파일을 받아 설치하고
실행합니다. 처음 켜면 준비에 몇 분 걸리며, 작업 표시줄 아이콘이 안정되면 준비가 끝난 것입니다.
설치 중 Kubernetes 사용 여부를 물으면 꺼도 됩니다.

설치가 잘 됐는지 확인하려면 압축을 푼 폴더(아래 2단계)에서 `scripts\check-setup.bat`을
더블클릭하세요. 무엇이 준비됐고 무엇이 남았는지 한국어로 알려줍니다.

### 2. 도구 내려받기

GitHub 저장소 페이지에서 초록색 Code 버튼을 누르고 Download ZIP을 고른 뒤, 받은 파일의
압축을 풉니다. 압축을 푼 폴더 안에 `scripts` 폴더가 보이면 됩니다.

### 3. 웹 UI 실행

`scripts` 폴더의 `sbom-ui.bat`을 더블클릭합니다. 잠시 뒤 브라우저에 `http://localhost:8080`
화면이 열립니다.

처음 실행하면 검은 창에 "스캐너 이미지를 내려받습니다(약 3~4GB)"라는 안내가 나오고 다운로드가
진행됩니다. 네트워크에 따라 몇 분 걸릴 수 있으니 창을 닫지 말고 기다리세요. 한 번 받아 두면
다음부터는 바로 열립니다. 결과는 `C:\Users\사용자이름\sbom-output` 폴더에 저장됩니다.

<!-- TODO(windows-capture): sbom-ui.bat 첫 실행 시 다운로드 안내가 뜬 검은 창 스크린샷 -->

![SBOM Generator 웹 UI](images/web-ui.png)

### 4. 소스 ZIP 올리고 스캔

1. 프로젝트 이름과 버전을 입력합니다.
2. 스캔 대상에서 "ZIP 업로드"를 고릅니다.
3. 개발팀에게 받은 소스 코드 ZIP 파일을 올립니다.
4. 스캔 실행을 누릅니다. 진행 로그가 실시간으로 표시됩니다.

![스캔 진행 화면](images/web-ui-scan.png)

### 5. 고지문 내려받기

스캔이 끝나면 결과 목록에서 `..._NOTICE.txt`와 `..._NOTICE.html`을 내려받습니다. 함께 만들어진
SBOM(`..._bom.json`)과 위험분석 보고서(`..._risk-report.html`)도 같은 화면에서 받을 수 있습니다.
내려받은 파일은 위에서 안내한 결과 폴더에도 저장됩니다.

<!-- TODO(windows-capture): 결과 다운로드 목록에서 NOTICE 파일이 보이는 화면 -->

## 막혔을 때

- **검은 창에 "Docker가 설치되어 있지 않습니다"가 떠요**: Rancher Desktop을 설치하고 실행했는지
  확인하세요. `scripts\check-setup.bat`을 더블클릭하면 지금 무엇이 문제인지 알려줍니다.
- **"Docker 엔진이 실행 중이 아닙니다"가 떠요**: Rancher Desktop을 켜고 아이콘이 안정될 때까지
  기다린 뒤 다시 `sbom-ui.bat`을 실행하세요.
- **스캔은 끝났는데 결과 폴더에 파일이 없어요**: 결과 폴더가 Docker의 파일 공유 범위 밖이면
  생길 수 있습니다. 이 도구는 홈 디렉터리(`C:\Users\...`) 아래 `sbom-output`에 저장하므로 보통
  안전합니다. 그래도 보이지 않으면 브라우저 화면의 다운로드 버튼으로 직접 받으세요.
- **브라우저가 자동으로 안 열려요**: 주소창에 `http://localhost:8080`을 직접 입력하세요.

더 자세한 설명과 명령줄 사용법은 [시작하기](getting-started.md)와
[고지문·보안·UI](notice-security-ui-guide.md) 문서를 참고하세요.
